var $entry = $(".entries [data-schemable-type='Comment'][data-schemable-id='<%= @comment.id %>']")
               .parent(".entry-li")
               .html("<%=j render @comment.entry %>")
               .find(".entry");

initializeEntry( $entry );

new Foundation.Tooltip( $entry.find("[data-tooltip]") );