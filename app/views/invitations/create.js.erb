if (parseInt("<%= @processor.errors.size %>") > 0) {
  postInvitationErrors("<%=j @processor.errors.to_json.html_safe %>");
} else {
  $(".invitations-list").find('.invitation').remove();
  $(".invitations-list").append("<%=j render @scheme.invitations %>");
}